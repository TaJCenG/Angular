import { BasicHighlighter } from './basic-highlighter';

describe('BasicHighlighter', () => {
  it('should create an instance', () => {
    const directive = new BasicHighlighter();
    expect(directive).toBeTruthy();
  });
});
