import { SafeRender } from './safe-render';

describe('SafeRender', () => {
  it('should create an instance', () => {
    const directive = new SafeRender();
    expect(directive).toBeTruthy();
  });
});
