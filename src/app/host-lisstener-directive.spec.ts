import { HostLisstenerDirective } from './host-lisstener-directive';

describe('HostLisstenerDirective', () => {
  it('should create an instance', () => {
    const directive = new HostLisstenerDirective();
    expect(directive).toBeTruthy();
  });
});
