import { HostBinding } from './host-binding';

describe('HostBinding', () => {
  it('should create an instance', () => {
    const directive = new HostBinding();
    expect(directive).toBeTruthy();
  });
});
